<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Pacientes</title>
</head>
<body>
  <h1>Lista de Pacientes</h1>
  <ul>
    <% pacientes.forEach(p => { %>
      <li><%= p.nombre %> <%= p.apellido %> - <%= p.cedula %></li>
    <% }) %>
  </ul>
</body>
</html>
